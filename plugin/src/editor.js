/**
 * Editor bundle — registers every block's editor side.
 *
 * Adding a component: create plugin/src/blocks/<name>/, then add one import here.
 * The PHP side discovers blocks by scanning for block.json, so nothing else changes.
 */
import './blocks/section-heading';
