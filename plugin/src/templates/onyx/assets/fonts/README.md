These Latin font files were extracted from the user-supplied
`doc/client-html/theme/Onyx Template (standalone).html` for visual parity.
Archivo supplies display text; Instrument Sans supplies body text. The template
stylesheet declares the same normal weight ranges used by the supplied canvas.
Vite includes the files in the scoped customer CSS, without third-party requests.
